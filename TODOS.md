# eyeballs TODOs

## Ship
- [ ] File npm name dispute for `eyeballs` (squatted by dead package, v0.0.0, 1+ year old)
- [ ] List on MCP registries (Anthropic, mcp.run, Smithery)

## Bugs / Polish
- [ ] Fix hardcoded version `0.1.0` in mcp.ts Server constructor (should read from package.json)
- [ ] Dark strip at top of CNN screenshot after overlay dismiss (ad/video placeholder still visible)

## Features
- [ ] Element detection mode (CSS selector monitoring, needs live page)
- [ ] AI detection mode (Claude vision evaluation, needs API key + billing)
- [ ] `--watch N` flag for CLI check command (re-check every N seconds)
- [ ] Full-page screenshot option (`--full-page`)

## Infrastructure
- [ ] Tiny Daemons brand (tinydaemon.com owned, npm `@tinydaemons` available, back pocket for now)
- [ ] eyeballs API (parked at danecodes/eyeballs-api, not active)
